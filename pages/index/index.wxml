<navigator class="media-box" wx:for="{{ entities }}" wx:key="{{ item.id }}">
  <view class="media-box__bd">
    <view class="media-box__title">
      {{ item.title.rendered }}
    </view>
    <view class="media-box__desc">
      {{ item.excerpt.rendered }}
    </view>
  </view>
</navigator>
